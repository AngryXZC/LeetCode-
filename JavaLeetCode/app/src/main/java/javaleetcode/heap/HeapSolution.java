package javaleetcode.heap;

public class HeapSolution {
    /**
     * 在介绍 堆 之前，我们需要首先介绍下 优先队列。
     *
     * 维基百科：优先队列是计算机科学中的一类抽象数据类型。
     * 优先队列中的每个元素都有各自的优先级，优先级最高的元素最先得到服务；优先级相同的元素按照其在优先队列中的顺序得到服务。
     *
     * 在生活中，我们处理任务的时候总是会给任务区分优先级。首先我们会处理优先级高的任务，接着我们会处理下一个优先级高的任务。这其实就是一个 优先队列 。
     *
     * 在很多书中会认为 堆 就是 优先队列 。
     * 但是， 优先队列 是一种抽象的数据类型，
     * 而 堆 是一种数据结构。所以 堆 并不是 优先队列 ，
     * 堆 是实现 优先队列 的一种方式。
     *
     * */


    public  static void main(String[] args){

    }
}
